基于 BERT 的文本编码与相似度计算技术方案
模型与环境准备：部署 PyTorch 框架，选用bert-base-chinese预训练模型（适配中文客服语料），加载预训练权重后，针对业务语料微调顶层编码层，提升领域语义捕捉能力。
文本预处理：对用户提问 / FAQ 文本统一处理，通过 BERT Tokenizer 分词，添加 [CLS]（句首标识）、[SEP]（句尾标识），按 128token 长度做 Padding（补 0）或 Truncation（截断），生成输入 ID、注意力掩码等模型输入张量。
BERT 编码：将预处理张量输入 BERT 模型，经 Transformer 层编码后，提取所有 token 隐藏层向量的均值池化结果（或 [CLS] 位置向量），生成 768 维语义向量，完整表征文本语义。
相似度计算：对向量做 L2 归一化（消除长度影响），通过余弦相似度公式（相似度 = 向量点积 / 向量模长乘积）计算相似度值（0-1），设定 0.8 为阈值，筛选 Top-N 高相似度 FAQ 返回。
