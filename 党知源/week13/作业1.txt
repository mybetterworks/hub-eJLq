规模：总参 236B → 671B，每 token 激活 21B → 37B；预训练数据量更大（约 14.8T tokens），数据里数学/代码、多语种比例和清洗也有加强。
MoE 负载均衡：V2 主要靠辅助损失实现均衡，会影响效果；V3用无辅助损失（bias）做均衡，再配一个很小的序列级辅助项防极端情况，训练不降 token。
训练目标：在 next-token 之外加 多 Token 预测（MTP），训练信号更密；推理可以 不用 MTP，也可拿来 投机解码 加速。
门控细节：路由侧用 sigmoid 亲和度 + 对选中专家归一化 等，和 V2 具体公式略有区别（同属 DeepSeekMoE 一族）。
工程与精度：大规模上完整跑通 FP8 训练 + DualPipe / 通信与计算重叠 等，把 跨节点 MoE 训得又稳又省。
后训练：SFT + RL，并从 R1 系列蒸馏推理，.chat 能力更强。
